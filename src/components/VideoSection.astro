---
import { overlay } from '../config';

interface Props {
	slug: string;
	title: string;
	youtube: string;
	blurb?: string;
}

const { slug, title, youtube, blurb } = Astro.props;
const showBlurb = overlay.show && overlay.showBlurb && Boolean(blurb);
---

<section
	class="video"
	data-video-section
	data-video-id={youtube}
	data-slug={slug}
	aria-label={title}
>
	<div class="video__crop">
		{/* The IFrame API swaps this div out for the iframe it creates. */}
		<div id={`yt-${slug}`} data-video-mount></div>
	</div>

	<button class="video__tap" data-video-tap type="button" aria-label={`Play or pause ${title}`}
	></button>

	{
		overlay.show && (
			<div class="overlay">
				<h2 class="overlay__title">{title}</h2>
				{showBlurb && <p class="overlay__blurb">{blurb}</p>}
			</div>
		)
	}

	<button
		class="comment-button"
		type="button"
		data-comment-open={slug}
		data-comment-label={title}
		aria-label={`Comments on ${title}`}
	>
		<svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
			<path
				d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"
			></path>
		</svg>
	</button>
</section>
